{
  "name": "@albertli/openfin-browser-adapter",
  "version": "0.10.5",
  "description": "browser mockup adapter for openfin js api",
  "main": "index.js",
  "scripts": {
    "build":"yarn prebuild && yarn build:es2015 && yarn build:copy-files",
    "prebuild":"rimraf build",
    "build:es2015":"tsc src/index.ts --outDir build -d --declarationDir build",
    "build:copy-files":"node ./scripts/copy-files.js",
    "release":"yarn build && npm publish build --registry=https://albertli.ddns.net:9004/repository/npm-private/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": {
    "name": "Wentao Li",
    "email": "liwentao90@yahoo.com"
  },
  "license": "MIT",
  "dependencies": {
    "debug": "^3.1.0",
    "socket.io-client": "^2.1.1"
  },
  "devDependencies": {
    "@types/debug": "^0.0.30",
    "@types/jest": "^23.1.2",
    "@types/socket.io-client": "^1.4.32",
    "copyfiles": "^2.0.0",
    "fs-extra": "^6.0.1",
    "jest": "^23.2.0",
    "rimraf": "^2.6.2",
    "ts-jest": "^22.4.6",
    "typescript": "^2.9.2"
  }
}
